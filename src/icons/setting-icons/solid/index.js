/**
 * WordPress dependencies
 */
import { SVG, Path } from '@wordpress/primitives';

const solid = (
	<SVG xmlns='http://www.w3.org/2000/svg' viewBox='0 0 62 4'>
		<Path fill='var(--maxi-black)' d='M0 0h62v4H0z' />
	</SVG>
);

export default solid;
