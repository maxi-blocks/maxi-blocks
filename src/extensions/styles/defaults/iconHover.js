export const iconHover = (() => {
	const response = {
		'icon-status-hover': {
			type: 'boolean',
			default: false,
		},
		'icon-color-hover': {
			type: 'string',
		},
	};

	return response;
})();
