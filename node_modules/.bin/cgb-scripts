#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cgb-scripts/bin/cgb-scripts.js" "$@"
  ret=$?
else 
  node  "$basedir/../cgb-scripts/bin/cgb-scripts.js" "$@"
  ret=$?
fi
exit $ret
