/**
 * Protocol 2: Variable Enforcement (Style Card) — shared across 15/17 block prompts.
 * Button and Container have block-specific Protocol 2s; they define their own inline.
 */
export const PROTOCOL2_VARIABLE = `### PROTOCOL 2: VARIABLE ENFORCEMENT (Style Card)
- Never use hex codes unless explicitly asked.
- Prefer global variables: var(--bg-1), var(--bg-2), var(--highlight), var(--h1), var(--p).`;
