# @wordpress/* and react-leaflet disagree on nested react/react-dom versions; npm 7+ strict peers
# would block install without this. Matches resolution used for npm audit fix and lockfile.
legacy-peer-deps=true
